---
interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<span class="relative" class:list={[className]}
  ><span
    class="absolute -z-1 top-[30%] left-0 w-full h-[40%] bg-teal-600/30 dark:bg-teal-400/30 -rotate-3"
  ></span><slot /></span
>
